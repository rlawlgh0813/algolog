spring:
  application:
    name: algolog

  profiles:
    default: local

  jpa:
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

algolog:
  jwt:
    access-token-expiration-millis: ${JWT_ACCESS_TOKEN_EXPIRATION_MILLIS:3600000}
